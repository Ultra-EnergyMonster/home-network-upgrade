# Router Configuration

## LAN Address

The router LAN gateway was changed during the upgrade:

Before:
192.168.0.1

After:
192.168.50.1

## DHCP

DHCP was enabled to automatically assign local IP addresses to connected devices.
